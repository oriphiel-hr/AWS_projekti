# Frontend Environment Variables

# API URL za backend
# Development (lokalno):
VITE_API_URL=http://localhost:4000

# Production (Hostinger - ako je backend na istom serveru):
VITE_API_URL=https://uslugar.oriph.io/api

# Production (Hostinger - ako je backend na subdomeni):
VITE_API_URL=https://api.uslugar.oriph.io

# Production (AWS ECS):
VITE_API_URL=https://uslugar.api.oriph.io

# Napomena:
# - NE stavljaj trailing slash (/)
# - URL treba biti samo do baze (bez /api na kraju ako se već dodaje u kodu)
# - Kopiraj ovaj fajl u .env za development

