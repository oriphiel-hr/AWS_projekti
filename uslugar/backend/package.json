{
  "dependencies": {
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  },
  "scripts": {
    "migrate:deploy": "prisma migrate deploy --schema=/app/prisma/schema.prisma",
    "migrate:status": "prisma migrate status --schema=/app/prisma/schema.prisma",
    "generate": "prisma generate --schema=/app/prisma/schema.prisma"
  }
}
