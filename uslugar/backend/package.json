{
  "dependencies": {
    "@prisma/client": "5.22.0",
    "express": "4.19.2"
  },
  "devDependencies": {
    // makni "prisma" odavde ako je bio
  },
  "prisma": { "schema": "prisma/schema.prisma" },
  "scripts": {
    "start": "node server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate:deploy": "prisma migrate deploy"
  }
}

